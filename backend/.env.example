# 選擇後端：postgresql 或 sqlite
DB_BACKEND=sqlite

# PostgreSQL 設定（DB_BACKEND=postgresql 時使用）
DB_ENGINE=django.db.backends.postgresql
DB_NAME=platform
DB_USER=user
DB_PASSWORD=your_password
DB_HOST=127.0.0.1
DB_PORT=5432

# SQLite 設定（DB_BACKEND=sqlite 時使用）
# SQLITE_NAME=db.sqlite3
